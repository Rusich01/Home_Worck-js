const array = ['lenovo', 'dell', 'apple', 'asus', 'acer'];
const arrayOfUsers = ['user', 'user1', 'user10', 'user100', 'user1000'];

// Завдання № 1
// function audit(item) {
//   console.log(item, item.length);
// }
// array.forEach(audit);

// Скороченний  запис
// array.forEach(item => console.log(item, item.length));

// Завдання № 2
// const giveWord = prompt('Який бред обиреш?').toLowerCase();

// const search = (a, b) => {
//   return console.log(a.includes(b));
// };

// search(array, giveWord);

// Завдання № 3
// console.log(arrayOfUsers.find(item => item.length > 4));

// Завдання № 4
// console.log(arrayOfUsers.filter(item => item.length > 4));
