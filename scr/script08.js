const users = ['user1', 'user2', 'user3', 'user4', 'user5', 'user6'];

// Завдання №1
// Вирішив зробити через forEach:

// console.log(
//   users.forEach(item =>
//     item.length >= 4 ? console.log(item, true) : console.log(item, false),
//   ),
// );

// console.log(users.every(item => item.length > 4));

// Завдання №2
// console.log(users.some(item => item.length > 4));

// Завдання №3
// console.log(users.map(item => item.toUpperCase(item)));
